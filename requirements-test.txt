# Reproducible local and CI validation environment for Strick Tech Agentic OS.
-r requirements.txt
pytest>=8.0,<10.0
pytest-asyncio>=0.24,<1.0
beautifulsoup4>=4.12,<5.0
playwright==1.49.0

# ── Static analysis (CI quality gate) ─────────────────────────────────────────
# Configuration lives in pyproject.toml under [tool.ruff]. CI only enforces
# the F821 (undefined-name) rule as a hard blocking gate — see ci.yml for
# rationale. The full configured rule set (E, W, F, I, N, UP, B, S, T20, SIM)
# remains available for local `ruff check .` runs and incremental cleanup.
ruff>=0.16,<0.17
