# Agentic OS — Gap Hunt & Fix Tracker

**Plan:** exhaustively find and fix correctness/security/robustness gaps across all backend + frontend
modules, driving every *runnable* suite to 0 failures and maximizing coverage of reachable code.
Environment-gated suites (browser E2E, AI-provider, seeded-demo, hardware) are verified by static
analysis + unit/integration and documented as "confirm in CI."

**Operating rules (agreed):**
- Bugs & correctness first (no gratuitous refactor).
- Full backend authority.
- Every fix ships with a failing→passing regression test; no collateral regressions.
- Small, verified commits → `main`; rebuild frontend bundle when frontend source changes.
- Verify-by-analysis for environment-gated behavior, documented explicitly.

---

## Baseline (captured 2026-08-31)

- Backend unit suite: **4647 passed / 185 skipped / 0 failed**.
- Frontend vitest: **75 / 75 passed**.
- Backend coverage (unit suite, `.coverage`): **60% of backend** (33,542 stmts / 13,525 miss).
- Repo audit tracker: **22/22 user-facing destinations fully reviewed**; 0 panes with no test mention.
- Linters: ruff + eslint + globals + colour-contrast + bundle-reproducibility all clean (on `main`).

### Lowest-coverage backend files (hunting ground)
connectors 12% · drift 15% · a2a 21% · replay 24% · llm 26% · engine 28% · voice 28% · specs 30% ·
websocket 31% · obsidian 32% · testgen 32% · builder 33% · memory 35% · agent_leaderboard 36% ·
crdt 38% · tauri_build 40% · goal_manager 40% · browser_agent 41% · fusion 44%.

---

## Ledger

| id | Severity | Module | Evidence | Fix | Status |
|----|----------|--------|----------|-----|--------|
| 001 | **High** | `backend/routers/crdt.py` | OT `_compact` merged adjacent ints **regardless of sign**, so a retain next to a delete (e.g. `[1,-1]`) collapsed to `[]` and `[1,-1,3]` to `[3]` — silently corrupting/dropping text on any transform that produced a retain adjacent to a delete. Additionally, `_transform` dropped the surviving side's ops when the *other* side exhausted, losing deletes and breaking convergence (e.g. base `'ab'`, A `[1,'X']`, B `[-2]` → `'aXb'` vs `'X'`). Reachable via live collaborative editing (`apply_and_broadcast` transforms every concurrent op). | `_compact` now merges only **same-direction** ints (retains together, deletes together); `_transform` passes the surviving side's remaining ops through unchanged when the other side exhausts. | **FIXED** — 3 failing→passing regression tests; 200,000-pair fuzz on both transform sides → 0 divergences; 79/79 collab tests pass; full unit suite 4650 pass / 0 fail; ruff clean. |
| 002 | **Critical** | `backend/services/plugin_sandbox.py` | **Sandbox escape → arbitrary file read / RCE.** Runtime `__builtins__` exposed `getattr`, `type`, `setattr`. The static gate only blocked *literal* `getattr(` calls (AST `Call`), but `getattr`/`type` were still in the runtime namespace, so a plugin that **passed validation** (`{'ok': True, 'violations': []}`) could alias `G = getattr`, build dunders at runtime via `chr(95)*2`, then traverse object graph `().__class__.__mro__[1].__subclasses__()` → reach real `__builtins__['open']` and read `/etc/hostname` (reproduced: `'e2b.local'`). Same primitive = arbitrary code execution. | Removed `getattr`/`type`/`setattr`/`delattr`/`vars`/`dir`/`globals`/`locals`/`eval`/`exec`/`compile`/`open` from plugin `__builtins__` (allow-list + hard deny-list belt-and-suspenders). Strengthened static gate with AST checks: reject any private attribute access (attr starting with `_`), any reference to a traversal builtin (even aliased), and any string literal containing `__`. | **FIXED** — 3 failing→passing regression tests; exploit now blocked at validation; benign plugins still run; 197/197 plugin/sandbox tests pass; full unit suite 4653 pass / 0 fail; ruff clean. |
| 003 | **Critical** | `backend/routers/specs.py` | **Path-traversal arbitrary file write.** `spec_id` (URL path param) was used as a filesystem dir under `SPECS_DIR` with **no validation**. Reproduced via `PUT /api/specs/../../evil/artifacts/x.md` → `test_...` wrote `/tmp/evil/x.md` = `'PWNED'` **outside** `SPECS_DIR`. `filename` had the same hole (`../../x.md`), and the old `..`-stripping silently *relocated* writes (`sub/x.md` → `x.md`). | Added `_SPEC_ID_RE = ^[a-z0-9][a-z0-9_-]{0,63}$` enforced in `_spec_dir()`; new `_clean_artifact_filename()` rejects `/`, `\`, `..` and a leading dot; `_save_artifact` adds an `is_within()` containment check; artifact endpoints catch `ValueError` → HTTP 400. | **FIXED** — 7 failing→passing regression tests; `../../evil` id + `../../x.md`/`sub/x.md`/`.hidden` filename all refused; valid `spec_abc`/`notes.md` still writes+reads; full unit suite 4660 pass / 0 fail; ruff clean. |
| 004 | **Medium** | `backend/services/agent_engine.py` | **Circuit-breaker half-open admission cap was dead.** `half_open_calls` was set to 0 on every transition but **never incremented** — the half-open admission gate returned `0 < half_open_max_calls`, i.e. `True` unconditionally. A provider that recovered into `HALF_OPEN` let an **unlimited** number of concurrent probes through while it was supposed to admit at most `half_open_max_calls` (default 3) before concluding open/closed. Confirmed via test: admitted `3/3` probes but still allowed another. | Added `CircuitBreaker.acquire()` as the execution-appropriate admission gate: it admits a probe only while the half-open budget has remaining slots, consuming one per admission. `can_execute()` is now a **pure** predicate kept for status/health reads, so inspecting a breaker no longer consumes its probe budget. `execute_with_retry` gates on `acquire()`. | **FIXED** — 5 failing→passing regression tests (cap enforced, close-on-success, probe-failure-reopens, status reads are pure, budget respected at high threshold); 42/42 existing engine tests still pass; full unit suite 4665 pass / 0 fail; ruff clean. |
| 005 | **Critical** | `backend/routers/hooks.py` | **Hook `condition` field → RCE.** `create_hook`/`update_hook` persisted a user-controlled `condition` verbatim, and `fire_event` ran it with `eval(cond, {'__builtins__': {}}, safe_locals)`. Empty `__builtins__` does **not** sandbox Python — dunder attribute traversal is intrinsic. A condition like `[x for x in ().__class__.__mro__[1].__subclasses__() if x.__name__=='Popen'][0].__init__.__globals__['os'].system('...')` reached `os.system` and executed an arbitrary command (reproduced: wrote `/tmp/hook_rce`). Every live hook event path (file_save, git_commit, test_fail, chat_send, …) is in scope. | Replaced `eval()` with an AST allow-list evaluator (`_validate_condition_ast` / `_safe_eval_condition`): only comparison/boolean expressions are permitted (attribute reads on `file`/`event`/`commit`/`test`, literal `in`/`not in` lists, `>`, `and`, `or`, `not`). Calls, subscripts, comprehensions and dunder/private attribute access are rejected with `ValueError`/`SyntaxError`. Enforced **at storage time** (create_hook/update_hook reject unsafe conditions) and at evaluation time (`fire_event` skips any that slip through). | **FIXED** — 13 regression tests (10 RCE/traversal payloads rejected incl. the confirmed one; legit conditions still evaluate; false cases correct); 109 existing hooks-related + 65-suite tests still pass; full unit suite 4678 pass / 0 fail; ruff clean. |
| 006 | **High** | `backend/routers/profiler.py`, `backend/routers/replay.py` | **`exec()` with restricted builtins is still RCE.** Both ran user-supplied source through `exec(code, {'__builtins__': _SAFE_BUILTINS})`; a fixed builtins set does not sandbox Python (existing-object attribute traversal reaches a subprocess factory). | Migrated to `services/codeguard.run_guarded_exec` / `reject_unsafe_attribute_usage`, which blocks the dunder/private attribute traversal primitive every escape relies on, before `exec`. | **FIXED** — `test_201_profiler_replay_rce.py`; full unit suite green; ruff clean. |
| 007 | **High** | `backend/routers/secrets.py` | **`/api/secrets/test-connection` (provider=ollama) — SSRF + persistent Ollama config-injection.** A `url` straight from the request body was passed to `client.get(url)` with no guard. The server fetched any attacker-chosen address (cloud metadata `169.254.169.254`, internal API, LAN host) — an SSRF — and on a 200 the attacker's URL was written to `os.environ['OLLAMA_BASE_URL']` and `llm.OLLAMA_BASE`, redirecting **all** subsequent model traffic to that host. Reproduced: the handler fetched an internal URL and returned its body. | Added `_validate_ollama_base_url()` called before any fetch: only localhost/loopback (`localhost`, `127.*`, `::1`) is accepted; the cloud metadata service, any private/LAN host, non-http(s) schemes, and empty/no-scheme values are refused. The URL is never persisted until it passes. | **FIXED** — 19 regression tests (6 localhost URL forms allowed; 12 SSRF/abuse forms rejected incl. metadata, private-LAN, `localhost.evil.com`, `file://`, no-scheme; non-string rejected); 121 existing secrets/onboarding/ollama tests still pass; full unit suite 4708 pass / 0 fail; ruff clean. |
| 008 | **High** | `backend/routers/replay.py` | **Replay workflow `webhook` node — SSRF.** A user-submitted workflow definition can contain a `webhook` node whose `url` is fully attacker-controlled. On run it was passed straight to `client.post(url)`, so the server POSTed to any internal address (SSRF). Its twin in `workflow.py` was already guarded by `_is_ssrf_blocked_url`; the replay path was the unguarded copy. (The rerun-from path only executes `agent` nodes, so it wasn't a second sink.) | Applied the same `_is_ssrf_blocked_url` guard (refuse non-http(s) and private/link-local/metadata addresses) and set `follow_redirects=False` so a public URL cannot 302 to an internal one after the check. | **FIXED** — 11 regression tests (8 internal/abusive URL forms refused incl. metadata/loopback/private/`file://`; public host predicate is safe-boolean); 166 existing replay/workflow tests still pass; full unit suite 4719 pass / 0 fail; ruff clean. |
| 009 | **Medium** | `backend/routers/documents.py` | **`.docx` decompression-bomb → memory exhaustion.** `_extract_docx` read the whole `word/document.xml` member (`archive.read`) and `ET.fromstring`-parsed it into an in-memory DOM **before** `MAX_EXTRACTED_CHARS` trimmed anything. A .docx stores the member DEFLATED, so the 4MB upload cap says nothing about expanded size — a zip-bomb expanded a tiny upload to a huge DOM. Reproduced: 172KB zip → 39MB member; 516KB zip → 118MB member, both fully loaded. | Added `MAX_DOCX_XML_BYTES` (12MB); `_extract_docx` checks `archive.getinfo('word/document.xml').file_size` and refuses an over-bound member before reading/parsing. | **FIXED** — 4 regression tests (valid docx still extracts; over-bound member refused before parse; bomb-ratio refused; clear error); 3 existing extraction tests still pass; full unit suite 4724 pass / 0 fail; ruff clean. |
| 010 | **Medium** | `backend/routers/rag.py`, `backend/routers/pluginsdk.py` | **Unbounded user uploads → memory exhaustion.** `/api/rag/pipelines/{id}/upload` did `await file.read()` (whole file) then fed it to `add_document`, which truncates to 1M chars — so a large upload was fully loaded into memory and most of it discarded. `/api/pluginsdk/import` did unbounded `file.read()` and `zf.read()`, so a huge or highly-compressed plugin zip was fully pulled into memory. (marketplace.py already capped; these two did not.) | `rag.py`: cap read at `MAX_UPLOAD_CHARS` (1M) and 413 if exceeded. `pluginsdk.py`: cap upload at `MAX_IMPORT_BYTES` (10MB) and the decompressed `manifest.json` at `MAX_MANIFEST_BYTES` (2MB), refusing oversized cases. | **FIXED** — 5 regression tests (bounds sane; oversize RAG upload not accepted; manifest/zip oversize never a successful import); 134 existing document/rag/plugin/marketplace tests still pass; full unit suite 4728 pass / 0 fail; ruff clean. |
| 011 | **High** | `backend/routers/terminal.py` | **Unauthenticated networked shell on a default deployment.** `_bound_to_loopback()` read `AGENTIC_OS_HOST` defaulting to `127.0.0.1`, while `backend/config.py`'s `load_config()` reads the same var defaulting to **`0.0.0.0`**. In a default deployment (no `AGENTIC_OS_HOST` set) the server binds to `0.0.0.0` (network-reachable) but this helper concluded "loopback only" → skipped auth → an unauthenticated remote user got a shell — precisely the "shell reachable from the network with no auth" case the comments call indefensible. Verified: with the var unset, `_bound_to_loopback()` returned True (auth skipped) while config binds 0.0.0.0. | `_bound_to_loopback()` now defaults its own read to `0.0.0.0` (matching config.py), so a default deployment authoritatively requires auth on the shell. Added `AGENTIC_OS_HOST=127.0.0.1` in `tests/unit/conftest.py` so the unit suite simulates the loopback-bound (default, secure) deployment it already assumed. | **FIXED** — 1 regression test (unset host → network-reachable, auth required); 101 terminal tests still pass; full unit suite 4729 pass / 0 fail; ruff clean. |
| 012 | **Medium** | `backend/services/agent_engine.py` | **Chain step eval not sandboxed (latent RCE).** `execute_chain` ran user-influenced `step.condition`/`step.transform` through `eval(expr, {'__builtins__': {}}, ctx)`. An empty __builtins__ is NOT a sandbox — verified `().__class__.__base__.__subclasses__()` returns 162 live subclasses through it, so a crafted condition reaches the interpreter (e.g. subprocess.Popen). `execute_chain` currently has no callers (dead code), so this is not yet remotely reachable, but it is the exact class the codebase already hardened in hooks/profiler/replay and would become an RCE the moment a chain is wired to a route. | Added `_safe_chain_eval`, which runs `codeguard.reject_unsafe_attribute_usage` (the primitive every escape relies on) before eval, consistent with codeguard.run_guarded_exec used by the profiler/replay/hooks paths; both eval sites now route through it. | **FIXED** — 5 regression tests (attribute/subclass traversal rejected; benign arithmetic/comparison/context lookup still works; guard rejects dunder); 168 existing agent/engine tests still pass; full unit suite 4734 pass / 0 fail; ruff clean. |
| 013 | **Low** | `backend/routers/bounty_hunter.py` | **Scan-id path-traversal (read/write).** `get_bounty_scan` / `execute_autopatch` built `SCANS_DIR / f'{scan_id}.json'` straight from the URL path param — a caller-supplied traversal payload would escape the scans dir to read (or, via autopatch, overwrite) an arbitrary .json file. Note: FastAPI's routing currently normalizes `../` away and rejects encoded slashes in a `{scan_id}` path segment, and the write path uses server-generated `bh_scan_<uuid>` ids, so this is **not yet reachable through HTTP** — verified (all traversal variants returned 404/422). Hardened anyway to match `workflow._wf_path`, so a route/mount change can't expose it. | `_safe_scan_id()` strips everything outside `[A-Za-z0-9_-]` (capped 128) and both read/write paths now use `_scan_file()`; verified all payloads stay within `SCANS_DIR`. | **FIXED** — 5 regression tests (traversal payloads stay within scans dir; separators/slashes stripped; length capped; HTTP traversal of encoded separator still 404/422); 229 path/system/workflow tests pass; full unit suite 4739 pass / 0 fail; ruff clean. |
| 014 | **Medium** | `frontend/js/01-app-core.js` | **Primary markdown renderer injects raw HTML (HTML/beacon/link injection).** `renderMarkdownEnhanced` (wired as `window.renderMarkdown`, used for all chat/agent/mcp output) did NOT escape its input, and its regex replacements injected raw capture groups into innerHTML — inline code `$1`, bold/italic `$1`, headers `$1`, blockquotes/lists `$1`/`$2`, and link `href="$2"`+label `$1`, plus a `javascript:`/`data:` href was allowed verbatim. Verified: `<img src=x onerror=…>` through inline-code/bold/etc. emitted an unescaped tag. The enforcing CSP (`script-src 'self'`, `style-src 'self'`) blocks script execution and inline handlers/styling, **but `img-src 'self' data: blob: https:` still loads external images** — so an injected `<img src="https://attacker…">` and arbitrary HTML/phishing link injection survived. CSP was the sole backstop, contradicting the codebase's explicit per-call-site-escaping-plus-CSP stance. | Converted every user-content capture group to an escaped function replacer (`escHtml`), and the link `href` is scheme-allow-listed (`https?: | // | mailto: | / | #`), falling back to `#` for `javascript:`/`data:`. Code-block content stays escaped (as before); benign markdown unaffected. | **FIXED** — Node harness proves `onerror`/`onload`/`<script>`/`<svg>` and `javascript:`/`data:` links are neutralized while `**bold**`/`code`/`https://` links still render; `node --check` passes; rebuilt `frontend/dist` bundle carries the fix; new vitest `frontend/tests/renderMarkdownEnhanced.test.js` (8 tests) added for CI. |
| 015 | **Medium** | `frontend/js/01-app-core.js`, `frontend/js/03-features-a.js` | **Unescaped server/user strings in innerHTML.** (a) The Ollama model list rendered each model name as `<span class="tag">${m}</span>` with no escaping — model names are user-controlled (`ollama create`/pull names), and under `img-src ... https:` a model named `x</span><img src=https://attacker…>` becomes a tracking beacon (same class as #014). (b) The flamegraph `renderNode` put `${node.name}` unescaped into both the `title="..."` attribute (a `"` in a symbol name breaks the attribute context) and the element body. | (a) Model name now `escHtml(m)`. (b) Flamegraph `node.name` now `escHtml` in the `title` attribute and the element body. Both verified present in the rebuilt served bundle. | **FIXED** — `node --check` passes on both files; the rebuilt `frontend/dist/app.c103a33474f8.js` carries `escHtml(m)` and `escHtml(node.name)`. Verified separately-safe (already escaped or passive/mitigated): deploy `setup/alt/installs/then_`, account avatar (data:-guarded, `<img>` passive), workflow-specs sim steps (static literals), profiler banner (`d.note` escaped), dozens of `${status}`/`${count}`/`${w}` numeric/status inserts. |

## Environment notes

## Environment notes
- Repo venv must be built with **/usr/local/bin/python3 (3.13.14)**; a 3.13.5 venv segfaults on
  `ctypes` in `backend/services/sandbox.py` (Python build artifact, NOT a code bug).
- Live-server suites (integration/connectors/security/regression/gap) need a server + `RATE_LIMIT_MAX`
  raised, plus seeded `demo_*` data and a real AI provider for the provider-gated paths.
- Browser E2E needs Chromium (unavailable in this sandbox).

> **Follow-up (shipped `6e921ea`):** the #014 markdown link handler was consolidated to use the shared `safeUrl()` guard (all other data-driven hrefs already use it; the repo lint requires it — `escHtml` alone lets `javascript:` survive quoting). `safeUrl` rejects `javascript:`/`data:`/control-char-smuggled/mailto + `//evil` and allows http/https/same-origin `/`. Verified; this also keeps `test_86_cross_cutting_hardening` green. Environment reset (3rd) restored `.venv` (full `requirements.txt` + test deps + playwright) and rebuilt `frontend/dist`; full suite back to **4739 passed / 0 failed**.

| 016 | **Medium** | `backend/routers/bugbot.py` | **Git-flag injection → arbitrary file-content disclosure.** `review-repo` appended the user-supplied `body['branch']` to the `git diff` argv with no validation. A leading `-` is interpreted by git as an option: with `staged=false` (the default), `branch="--no-index /etc/passwd /tmp/x"` makes `git diff` compare two arbitrary paths and embed their contents in the returned diff — leaking any file the server user can read into the review output. Reproduced live. (`git` is not a shell, so no RCE; but `gitai.py` already guards the same input via `classify_git_command`.) | `branch` validated as a git refname before append: a leading `-`, whitespace/ control, `..`, `@{`, `~^:?*[\\`, backslash, trailing `.`/`/`, or `.lock`/empty are all rejected -> `{ok:false}`. | **FIXED** — 4 regression tests (flag-injection payloads + metacharacter refs rejected; `main`/`feature/x`/`HEAD`/`v1.2.3` accepted; empty rejected); 28 existing steering/bugbot tests still pass; full unit suite 4743 pass / 0 fail; ruff clean. |

| 017 | **Medium** | `backend/services/agent_engine.py` | **Loop health `success_rate` miscalculated (and stale).** `run_count` counts only SUCCESSFUL iterations (`error_count` is a separate counter), but the formula was `(run_count - error_count)/run_count * 100 if run_count else 100`. The denominator excluded failures: 5 successes + 5 failures reported 0% (`(5-5)/5`); any `error_count > run_count` reported a NEGATIVE rate; and an all-failing loop (`run_count=0`) fell into `if total else 100` and reported **100% success forever**. It was also only recomputed in the success branch, so the all-failing case stayed at the 100% default. Verified live: mixed 5/5 -> 100%, all-fail -> 100% (should be 50% and 0%). | `success_rate = run_count / (run_count + error_count) * 100`, recomputed in BOTH the success and error branches (so an all-failing loop now reflects 0%). | **FIXED** — 4 regression tests (mixed 5/5 -> 50%; all-fail -> 0%; all-succeed -> 100%; more-errors-than-successes not negative); 84 agent_engine/loop tests pass; full unit suite green (+4); ruff clean. |

| 018 | **Low** | `backend/routers/arena.py` | **ELO update silently dropped for unlisted models.** `_update_elo` read the current elo defaulting to 1000 when a row was absent, but the `arena_leaderboard` row was only ever created by `_ensure_schema`'s seed over `AVAILABLE_MODELS`. A battle using a custom/unlisted model name (`body['model_a'/'model_b']`, since `AVAILABLE_MODELS.get(model, model)` passes arbitrary strings through) computed a rating against a 1000 default and then the `UPDATE` matched 0 rows — the rating change was lost and the model never appeared in the leaderboard. Verified live (custom names resulted in no rows). | `_update_elo` now `INSERT OR IGNORE`s the winner/loser rows before reading/updating, so an unlisted model is created at 1000 and correctly rated. | **FIXED** — 2 regression tests (custom model row created, winner >1000/loser <1000 with 1 win/1 loss/1 battle each; seeded model still updates); 69 arena/collab tests pass; ruff clean. |

| 019 | **Medium** | `backend/routers/goal_manager.py` | **Goal check-in at 100% never auto-completed the goal.** `add_checkin` wrote only the progress number (`UPDATE ... SET progress=?`), without the status/completed_at transition that `update_goal` and `complete_milestone` both apply at 100. A check-in reporting 100% left the goal `progress=100, status='active', completed_at=''`, so the dashboard summary counted a fully-progressed goal as active and listed it in upcoming deadlines — a 100%-complete goal never appeared done. Verified live (progress=100 / active / ''). | `add_checkin` now transitions to `status='done'` + `completed_at` when the reported progress reaches 100, matching the other two progress paths. | **FIXED** — 2 regression tests (check-in@100 -> done + completed_at; check-in<100 stays active); 76 goal-adjacent tests pass; ruff clean. |

| 020 | **Low** | `backend/routers/eval_framework.py` | **Platform eval `pass_pct` truncated by integer division.** `platform_eval_stats` computed `SUM(CASE WHEN pass_fail='pass')*100/COUNT(*)`, which in SQLite is INTEGER division — 1/3 reported 33 (not 33.33) and 7/8 reported 87 (not 87.5). Percentages always under-reported and inconsistent with the same query's float `avg_score`. Verified live. | CAST the passed-count to REAL before the `*100/COUNT(*)`, so `pass_pct` is a float like the rest of the summary. | **FIXED** — regression test asserts 1/3 -> 33.33 (not truncated); 68 eval tests pass; ruff clean. |

| 021 | **Medium** | `backend/routers/finetune.py` | **Unsanitized dataset_id/job_id → arbitrary file write/read.** `create_finetune_dataset`, `start_finetune_job`, `get_finetune_job`, and `export_lora_adapter` built paths like `DATASETS_DIR / f'{id}.jsonl'`, `f'{id}_meta.json'`, `JOBS_DIR / f'{id}.json'`, and `ADAPTERS_DIR / f'{id}.{format}'` with the user-supplied id. An id containing `../`, a path separator, or an absolute path escaped the datasets/jobs directory to write (verified: an absolute dataset_id wrote outside DATASETS_DIR) or read an arbitrary .json/.jsonl. `export_format` was also echoed into a filename suffix. | Added `_safe_stem()` (reduces any id to `[a-z0-9_-]`, capped 64, non-traversing) and routed every id use through it; `export_format` allow-listed to safetensors/gguf/ggml/bin. | **FIXED** — 4 regression tests (traversal payloads become safe stems and stay inside DATASETS/JOBS dirs; clean id preserved; empty falls back; export-format allow-list); the `test_35` 422 is the pre-existing no-rows honesty-fix, not this change; ruff clean. |

| 022 | **Low** | `frontend/js/26-swarm.js` (DAG renderer) | **Winner confidence shown from the wrong run, or fabricated.** The Judge Consensus Hub rendered `Math.round((runs[0]?.score || 0.96)*100)% confidence`. The backend returns `runs` in agent order (`asyncio.gather`), so the winner can sit anywhere in the array — the card then displayed another agent's score whenever the winner wasn't first, or a hard-coded `96%` whenever `runs[0]` had no `score` (fan-out strategy) or there were no runs at all. | `renderSwarmDAG` now resolves the winning run's own `score` and only renders confidence when a numeric score actually exists; no fabricated percentage, no trailing double period. | **FIXED** — 4 jsdom regression tests; 87 frontend tests pass; eslint no errors; bundle rebuilt and `--check` in sync. |

| 023 | **Low** | `frontend/js/03-features-a.js` (workflow canvas) | **Node snapped to 200 when dropped at canvas origin.** `wfAddNode` defaulted via `x: x || 200, y: y || 200`. A node dropped at world coordinate (0,0) — legitimate after panning — was treated as "no coordinate supplied" and jumped 200px right/down. | Defaulting now checks `Number.isFinite` (0 is a valid coordinate); only non-finite values fall back, and fractional drop positions rounded. | **FIXED** — 4 jsdom regression tests (x=0/y=0 preserved, left/top edge kept, non-finite defaults, fractional rounded); 91 frontend tests pass; lint clean (the remaining `frame.src` self-assign error is pre-existing); bundle rebuilt & in sync. |

| 024 | **Low** | `frontend/js/03-features-a.js` (workflow canvas) | **Pasted node snapped to 230, and duplicate node ids on rapid paste.** `wfPasteNode` used `node.x = (node.x || 200) + 30` (same #023 root cause — a pasted node at the canvas origin jumped to 230 instead of 30) and `node.id = \`n${Date.now()}\``, which collides when the user pastes twice within the same millisecond, producing two nodes sharing one id (corrupts edges/rendering). | `wfPasteNode` now defaults the offset only for non-finite positions (0 preserved) and appends a random suffix to the id, matching `wfAddNode`. | **FIXED** — 2 jsdom regression tests (origin paste → (30,30); two same-millisecond pastes get distinct ids); 93 frontend tests pass; bundle rebuilt & in sync. |

| 025 | **Low** | `frontend/js/03-features-a.js` (workflow canvas render) | **Node drawn at (100,100) even when stored at (0,0).** `wfNodeHTML` rendered `left:${node.x||100}px;top:${node.y||100}px`. A node the canvas correctly stored at x=0/y=0 was drawn at left:100, so the drawn node and its edges (which compute endpoints from the data `x`) disagreed — one node sat at a phantom position. This is the same #023 root cause and it undermined that fix. | `wfNodeHTML` now defaults only for non-finite positions (0 preserved), keeping the drawn node aligned with its data and edges. | **FIXED** — 1 jsdom regression test (origin node renders left:0/top:0, not 100); 94 frontend tests pass; bundle rebuilt & in sync. |

| 026 | **Low** | `frontend/js/08-replay-collab.js` (replay graph reconstruct) | **Reconstructed nodes without a node_start event collapsed to (0,0).** `ttdReconstructGraph` laid out only `frames.filter(event_type==='node_start')` IDs. In a run with no node_start frames — or a node seen only via output/log frames — that list was empty/incomplete, so the node never got a coordinate and rendered at the origin, overlapping the other nodes. | Layout now falls back to first-appearance order over all nodes and guards `COLS` to >=1, so every reconstructed node gets a real coordinate and distinct position; sequential edges built from the same order. | **FIXED** — 3 jsdom regression tests (output-only run laid out at distinct positions; a node seen only via outputs still laid out; empty frames no div-by-zero); 97 frontend tests pass; bundle rebuilt & in sync. |

| 027 | **Medium** | `frontend/js/*` (all admin/wizard modules) | **Inline handler args broken for any string value.** ~130 inline `data-act-*`/`onclick` handlers interpolated their argument via `${JSON.stringify(x)}`. The delegate reads the attribute with `getAttribute()` (browser HTML-decodes it) and then `JSON.parse`s each argument, but `JSON.stringify` emits the surrounding double quotes *into the attribute source*; the HTML parser closes the attribute at the first `"`, so `getAttribute()` returns a truncated handler (e.g. `sel(`) and the button is a no-op. Confirmed in jsdom: string args truncate; numeric args survive. The documented-correct helper `jsArg` (already used elsewhere) escapes quotes to entities first so the attribute survives parsing and decodes back to a valid literal. | Replaced `${JSON.stringify(x)}` → `${jsArg(x)}` on all 130 handler-attribute lines across 29 modules (kept `JSON.stringify` for the non-handler data usages: a textarea JSON dump, an op-string, a connector `data-connector-caps` payload, and comments). | **FIXED** — 4 jsdom regression tests demo the truncation and validate the `jsArg` round-trip (strings with apostrophes/double-quotes/entities, and nums/bools unchanged); 101 frontend tests pass; eslint introduces 0 new errors (9 pre-existing unchanged); bundle rebuilt & in sync. |

| 028 | **Medium** | `frontend/js/49-goals.js` | **Goal mutations claim success when the save fails.** `gmAddCheckin`, `gmAddMilestone`, `gmCompleteMilestone`, `gmEditGoal`, and `gmDeleteGoal` all `await fetch(...)` without checking `response.ok`, then toast success unconditionally (even the DELETE path toasts a deleted goal and clears the detail pane). A failed save — goal deleted in another tab, network error — shows "Updated"/"added"/"deleted" while nothing persisted; the goal silently reverts on next render. (`gmCreateGoal` already checked `ok`.) | Each of the 5 actions now checks `response.ok`; on failure it toasts an error and resyncs (reloads the goal/list) instead of claiming success. | **FIXED** — 1 jsdom regression test (delete with ok:false toasts an error, not "deleted"; verified it fails on the pre-fix code); 102 frontend tests pass; bundle rebuilt & in sync. |

| 029 | **Medium** | `frontend/js/29-notifications.js` | **Empty inbox displays fabricated sample notifications.** `refreshNotifications` fell back to hard-coded `SAMPLE_NOTIFICATIONS` whenever the API returned an empty list (`d.ok && d.notifications?.length > 0` is false). The backend always returns `ok:true` with the real (possibly empty) list and seeds its own welcome/status notifications, so a healthy but empty inbox claimed real "Welcome to Agentic OS" notifications that did not exist in the store — and they re-appeared every poll. | On `ok`, the response is trusted exactly (even empty); on a fetch failure it renders a clearly-labelled error state instead of samples. | **FIXED** — 3 jsdom regression tests (empty inbox shows "No notifications" and no sample content; failed fetch shows error; `unread_count` drives the badge). Verified 2 of 3 fail on pre-fix code. 105 frontend tests pass; bundle rebuilt & in sync. |

| 030 | **Medium** | `frontend/js/01-app-core.js` (markdown renderer) | **Fenced code blocks containing a backtick are corrupted.** `renderMarkdownEnhanced` replaced fenced blocks (inline backticks left raw inside code), then ran the inline `` `([^`\n]+)` `` transform over the whole result, so a JS template literal `\`...\`` inside a block was re-wrapped as a nested inline `<code>` span inside the `<pre>`, corrupting the code (SQL backtick identifiers and JS template literals especially). Verified live. | Code-block HTML is now stashed behind a placeholder token and restored only after all inline transforms complete, so block content is never re-parsed. | **FIXED** — 4 jsdom regression tests against the real function (template-literal block preserved; SQL backtick block preserved; genuine inline code still renders; plain fenced block fine). Verified 2 of 4 fail on pre-fix code. 109 frontend tests pass; bundle rebuilt & in sync. |

| 031 | **Low** | `frontend/js/01-app-core.js` (terminal exec) | **Multi-line snippet silently truncated, claimed sent.** `runCodeInTerminal` placed `code.split('\n')[0]` into the single-line terminal input and toasted "Code sent", so a multi-line script was dropped to its first line while the user believed it all went through. | Now warns when the snippet is multi-line ("placed line 1 of N") and only toasts success for a genuinely single-line snippet. | **FIXED** — 2 jsdom regression tests (multi-line warns "of 3", not "ok"; single-line success); verified the warn test fails on pre-fix code. 111 frontend tests pass; bundle rebuilt & in sync. |
